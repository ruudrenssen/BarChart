@page "/"

@using Charts
@using Newtonsoft.Json.Linq;
@using System.IO;

@{
    JObject chartData = JObject.Parse(File.ReadAllText(@"./Data/dataset-4.json"));

    BarChart chart = new BarChart(chartData);
}

@((MarkupString)chart.Chart().ToString())